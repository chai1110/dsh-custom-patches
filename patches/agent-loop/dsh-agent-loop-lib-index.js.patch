--- @deepseek-ai/dsh-agent-loop/lib/index.js
+++ @deepseek-ai/dsh-agent-loop/lib/index.js
@@ -985,9 +985,11 @@
 				kind: "error",
 				error: error instanceof LlmError ? error.failure : {
 					message: errorChain(error),
-					code: "UNKNOWN"
+					code: "UNKNOWN",
+					__stack: (error && error.stack) ? error.stack : String(error)
 				}
 			};
+
 			this.throwError(error);
 		} finally {
 			try {
@@ -1025,7 +1027,12 @@
 				step,
 				message
 			}, intent);
-			if (firstAttempt) for (const message of decision.messages) this.session.append("user/message", message, { surfaceOp: "append" });
+			if (firstAttempt) for (const message of decision.messages) {
+				const tail = this.session.surface.nodes.at(-1);
+				const tailEvent = tail === void 0 ? void 0 : this.session.eventAt(tail);
+				if (tailEvent?.type === "user/message" && tailEvent.data.id === message.id) continue;
+				this.session.append("user/message", message, { surfaceOp: "append" });
+			}
 			firstAttempt = false;
 			const request = this.buildRequest(config, preparedCall, assembly.tools, startsRequestSeries, signal);
 			const live = new AssistantStreamAttempt(this.session.id, ++this.assistantAttemptCounter, () => ++this.assistantStreamRevision, turn, step, (frame) => {
